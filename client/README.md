# Service Client & Declarations

This is a package that is published to our private registry containing all type declarations for the service and potentially some helper functions for trigger endpoints.
